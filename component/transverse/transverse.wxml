<view class="container">
  <block wx:for="{{transversedata}}" wx:key="transversedata">
    <view class="tran_views">
      <image src="../../img/{{item.src}}.png" />
      <text>{{item.name}}</text>
    </view>
  </block>
</view>